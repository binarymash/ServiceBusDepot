namespace ServiceBusDepot.ConsoleHost.Features.Exit
{
    public class PageOption : Features.PageOption
    {
        public PageOption(string key) : base(key, "Exit", new Request())
        {
        }
    }
}
